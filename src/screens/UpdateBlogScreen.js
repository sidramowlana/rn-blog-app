import React from "react";
import { View, Text, StyleSheet } from "react-native";

const UpdateBlogScreen = () => {
  return (
    <View>
      <Text> UpdateBlog Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default UpdateBlogScreen;
